name: svc-authz CI/CD (dev)
on:
  workflow_dispatch:
  push:
    branches: ['main']
    paths:
      - 'apps/svc-authz/**'
      - '.github/workflows/svc-authz-ci.yml'
permissions:
  id-token: write
  contents: read
env:
  PROJECT_ID: hyperush-dev-250930115246
  PROJECT_NUMBER: 443512026283
  REGION: europe-west1
  REPO: europe-west1-docker.pkg.dev/hyperush-dev-250930115246/hp-dev
  IMAGE_NAME: svc-authz
  TF_DIR: infra/terraform/environments/dev
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Auth to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/${{ env.PROJECT_NUMBER }}/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: deploy-sa@${{ env.PROJECT_ID }}.iam.gserviceaccount.com
          create_credentials_file: true
          export_environment_variables: true
      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.5.7'
      - name: Build & Push (Cloud Build)
        working-directory: apps/svc-authz
        run: |
          TAG="${GITHUB_SHA::12}"
          FULL_IMAGE="${REPO}/${IMAGE_NAME}:${TAG}"
          gcloud builds submit --tag "${FULL_IMAGE}" .
          BUILD_ID=$(gcloud builds list --format='value(ID)' --filter='createTime>=-5m' --limit=1)
          echo "Cloud Build logs: $(gcloud builds describe $BUILD_ID --format='value(logUrl)')"
          echo "FULL_IMAGE=${FULL_IMAGE}" >> $GITHUB_ENV
      - name: Terraform init/plan/apply (dev)
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform --version
          terraform init -input=false
          # Get current svc-api-gateway image from existing deployment
          CURRENT_API_GATEWAY_IMAGE=$(gcloud run services describe svc-api-gateway --region=${{ env.REGION }} --format="value(spec.template.spec.containers[0].image)" --project=${{ env.PROJECT_ID }} || echo "europe-west1-docker.pkg.dev/hyperush-dev-250930115246/hp-dev/svc-api-gateway:86d172f99c4f")
          echo "Using current svc-api-gateway image: ${CURRENT_API_GATEWAY_IMAGE}"
          terraform plan -input=false -out=tfplan -var="svc_authz_image=${FULL_IMAGE}" -var="svc_api_gateway_image=${CURRENT_API_GATEWAY_IMAGE}"
          terraform apply -input=false -auto-approve tfplan
      - name: Smoke test /health & E2E trace test
        working-directory: ${{ env.TF_DIR }}
        env:
          GCP_PROJECT_ID: ${{ env.PROJECT_ID }}
        run: |
          URL=$(terraform output -raw svc_authz_url)
          echo "Service URL: ${URL}"

          # Health check test
          echo "=== Health Check Test ==="
          for i in {1..10}; do
            code=$(curl -s -o /dev/null -w "%{http_code}" "${URL}/health")
            if [ "$code" = "200" ]; then
              echo "✅ Health check OK (200)"
              break
            fi
            sleep 3
            if [ $i -eq 10 ]; then
              echo "❌ Health check failed"; curl -i "${URL}/health" || true; exit 1
            fi
          done

          # E2E Trace test
          echo "=== E2E Trace Test ==="
          sleep 5  # Allow OTel to initialize
          TRACE_RESPONSE=$(curl -s "${URL}/v1/trace-test")
          echo "Trace test response: ${TRACE_RESPONSE}"

          # Extract trace URL from response
          TRACE_URL=$(echo "${TRACE_RESPONSE}" | jq -r '.trace.traceUrl // empty')
          if [ -n "${TRACE_URL}" ] && [ "${TRACE_URL}" != "null" ]; then
            echo "✅ E2E Trace test successful!"
            echo "🔍 View trace: ${TRACE_URL}"
            echo "trace_url=${TRACE_URL}" >> $GITHUB_OUTPUT
          else
            echo "⚠️  Trace URL not found in response, but service is responding"
            echo "This may be normal if OTel is still initializing"
          fi
