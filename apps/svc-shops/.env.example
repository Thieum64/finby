# Shopify app configuration (development values only)
APP_URL=http://localhost:8080
SHOPIFY_API_KEY=dev_key
SHOPIFY_API_SECRET=dev_secret
SHOPIFY_WEBHOOK_SECRET=dev_webhook_secret
SHOPIFY_SCOPES=read_products,write_products
# Optional: override default data directory
# DATA_DIR=./.data
# Optional: adjust OAuth state TTL (in seconds)
# STATE_TTL_SECONDS=600
