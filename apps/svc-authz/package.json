{
  "name": "svc-authz",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r src/* dist/",
    "start": "node dist/index.js"
  }
}
